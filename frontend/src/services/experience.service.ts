import { notImplemented } from "@/types/common.types";
import type { Experience } from "@/types/experience.types";

export function getExperience(): Promise<Experience[]> {
  return notImplemented("Public experience data");
}
